apiVersion: v1
kind: Service
metadata:
  name: {{ include "oncall.integrations.fullname" . }}
  labels:
    {{- include "oncall.integrations.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oncall.integrations.selectorLabels" . | nindent 4 }}
